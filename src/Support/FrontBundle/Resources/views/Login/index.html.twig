{% extends 'SupportFrontBundle::layout.html.twig' %}
{% block stylesheets %}
    {{ parent() }}
{#    <link rel="stylesheet" href="{{asset('bundles/app/css/sgk_index.css')}}" />
    #}
{% endblock %}

{% block contents %}
    <div class="program_box">
    {% include 'SupportFrontBundle:Login:login.html.twig' %}
    <div>
{% endblock %}

{% block javascripts %}
    {{parent()}}
    <script>
        var naverState ="{{login.naverState}}";
        var naverAppId ="{{login.naverAppId}}";
        var Login = {
            switch : function(hide, show){
                $("#"+hide+"Pop").hide(500);
                $("#"+show+"Pop").show(500);
            }
        };

        $(function(){
            $("#contents").css({overflow : "hidden"});
            var height = $(window).height() - $("#header").height() - $("#footer").height();
            console.log($("#loginBg").height(), height);
            $("#loginBg").height(height-15);
        });      
    </script>
    <script type="text/javascript"  src="{{asset("bundles/support/js/login.js")}}"></script>   
    <script type="text/javascript"  src="{{asset("bundles/support/js/naverApi.js")}}"></script>  
    <script type="text/javascript"  src="{{asset("bundles/support/js/facebookLoginAPI.js")}}"></script>  
        
{% endblock %}      